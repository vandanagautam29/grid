'use strict';
// Register `stories` component, along with its associated controller and template
angular.module('stories').component('stories', {
    templateUrl: 'components/stories/stories.template.html',
    controller: function StoriesController() {
        
    }
});